## 1. 准备阶段

- [x] 1.1 备份现有的 `history/generate.py` 脚本
- [x] 1.2 检查 `docs/blog/` 目录结构，确认文件格式
- [x] 1.3 准备测试用的 Markdown 文件样本（包含图片引用）
- [x] 1.4 确认博客文章中的图片引用格式一致性

## 2. 核心功能实现

- [x] 2.1 实现 `scan_blog_directory()` 函数：递归扫描 `docs/blog/` 目录
- [x] 2.2 实现 Markdown 文件过滤：检测"老罗投资周记"关键字
- [x] 2.3 实现 `extract_image_reference()` 函数：使用正则表达式提取图片路径
- [x] 2.4 实现 `convert_to_public_url()` 函数：转换相对路径为公网 URL
- [x] 2.5 实现 `extract_metadata()` 函数：提取标题（优先级：frontmatter > H1 > 文件名）
- [x] 2.6 实现 `parse_date_from_path()` 函数：从图片路径或文件路径解析日期
- [x] 2.7 移除或重构旧的 `scan_attachments_directory()` 函数

## 3. 错误处理和容错

- [x] 3.1 添加文件读取错误处理（文件不存在、权限不足、编码问题）
- [x] 3.2 添加图片引用解析失败处理（跳过文件并记录警告）
- [x] 3.3 添加日期解析失败处理（使用文件名作为标签）
- [x] 3.4 实现日志输出：显示处理进度、成功/失败/跳过统计
- [x] 3.5 添加脚本结束时的统计信息汇总

## 4. 集成和更新

- [x] 4.1 更新 `main()` 函数：调用新的扫描逻辑
- [x] 4.2 更新命令行参数：支持 `--blog-dir` 参数（可选）
- [x] 4.3 更新 `render_html()` 函数：适配新的元数据结构
- [x] 4.4 更新脚本文档字符串：说明新的实现方式
- [x] 4.5 确保日期排序功能正常工作（倒序排列）

## 5. 测试和验证

- [x] 5.1 本地运行脚本：`python history/generate.py`
- [x] 5.2 验证生成的 HTML 文件：`history/static/index.html`
- [x] 5.3 在浏览器中打开生成的页面，检查显示效果
- [x] 5.4 验证所有图片链接是否正确且可访问
- [x] 5.5 验证日期排序是否正确（最新日期在前）
- [x] 5.6 验证元数据提取（标题、日期）是否正确
- [x] 5.7 测试错误处理：删除或修改某篇博客的图片引用，验证脚本容错性
- [x] 5.8 性能测试：验证脚本运行时间在可接受范围内

## 6. 文档和部署

- [x] 6.1 更新 `history/README.md`：说明新的实现方式和工作原理
- [x] 6.2 在 README 中添加图片引用格式说明
- [x] 6.3 更新模板和 HTML 生成逻辑（如需要）
- [x] 6.4 提交代码到 git：`git add history/`
- [x] 6.5 创建提交：清晰说明变更内容
- [x] 6.6 推送到 GitHub（如需要）
